export const appConfig = {
  // number of items per page, max 10
  itemsPerPage: 10,

  // joke image width and height in pixels
  imageWidth: 100,
  imageHeight: 100,

  // when submitting the joke with dryrun=true, joke is not saved to API
  dryRun: true,
}
